<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TainLong" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TainLong" parent="Base.Theme.TainLong">
        <item name="android:windowBackground">@color/tv_bg</item>
    </style>

    <style name="Theme.TainLong.CustomDialogFragment">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>

    <style name="Theme.TainLong.CustomDialogFragment.Restore">
        <item name="android:windowEnterAnimation">@null</item>
    </style>
</resources>